# 2026-09-07

## 全项目代码审查（产出 CODE-REVIEW.html）

对 hkg_flight/ 包 + hkg_flight.py 单文件 + 测试 + 文档做了全面审查，30 项发现，报告写入 `CODE-REVIEW.html`。

关键结论（供后续会话直接引用，不必重读）：
- **两套实现并存且已分叉**：单文件 `hkg_flight.py`(2378行) 与包 `hkg_flight/`。单文件版更新（有 `esc()` XSS 转义 + CSS 变量暗色主题 + 包装式 API 契约 `{date,count,flights}`）；包版是回退版本（裸数组 + 无转义）。拆分时丢失了修复。
- **测试 100% 无法运行**：`test_hkg_flight.py` 从包导入 `Poller/search_flights/flights_for_date/load_airlines`，但 `__init__.py` 未导出 → ImportError。PROJECT-STATUS.md 里"58 个测试全部通过"是失效记录（58 个 test 方法确实存在）。
- **P0 缺陷**：Web XSS(web.py:320)、路径穿越任意文件写(web.py:149→api.py:66→cache.py:44, date 参数无校验)、curses TUI 按 W 崩溃(tui.py:545 `ch.lower()` 对 int 调用)、简单 TUI alerts/airlines UnboundLocalError(tui.py:122-131)。
- **P1 缺陷**：`--port/--cache-dir` 的参数值未被移除(cli.py:395) 导致 `--web --port 9000` 完全不可用；curses render() 不按 mode 分发导致告警/航司视图空白；0.0.0.0 无鉴权；CORS `*`;`--force`+`--cache-dir` 可 rmtree 任意目录；缺 `server_close()`。
- 其他：SSE 只发心跳不推数据；`log()` 只有日期没有时间；`status_pair`/`read_state`/`_make_snapshot` 等多处死代码；25 处未使用导入；无 pyproject.toml。
- 环境注意：本机 Python 3.13.12 **没有 pytest**，用 `python -m unittest test_hkg_flight` 跑测试。

## 项目约定/事实

- 数据源：`https://www.hongkongairport.com/flightinfo-rest/rest`，缓存 `~/.hkg_flight_cache/`，轮询 30s，限速 0.6s/请求。
- 文档现状：README.md 描述单文件用法（英文），COMMANDS.md / PROJECT-STATUS.md 描述包用法（中文）——文档已漂移，README 键位表与 TUI 实现不符。
- 报告评分：安全 D / 实用 C+ / 简洁 D / 美观 B- / 可维护性 D。建议：保留包结构、删除单文件、移植单文件版 Web UI、CLI 迁移 argparse。
