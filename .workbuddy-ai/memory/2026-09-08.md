# 2026-09-08

## 复审 v5（基线 8beb22f → 03fa20c，6 个提交）

产出 `CODE-REVIEW-v5.html`。**只读审查，用户明确要求不得改动任何项目文件**（本次仅新增报告文件）。

### 闭环情况
- v2 的 6 项新引入问题（N1-N6）**全部修复**：`_validate_date` 收敛到 `utils.validate_date`；`_esc` 死代码删；`shutil` 死导入删；参数错误改 ValueError→400 + 前端 `res.ok`/`Array.isArray`；文档 `web --force` 改为 `--force web`；`--force` 改 `api.bypass_cache`（不再清缓存）。
- v2 的 12 项未处理（U1-U12）**修复 10 项**：curses render 按 mode 分发、过滤输入（含 0 键笔误）、scroll 连带 ↑↓ 一并删除、cmd_search 接入 filter / cmd_date 删除、SSE 端点删除、告警 MAX_HISTORY=500、log 加时间、25 处未用导入**实测归零**、CLI 表格截断、新增 pyproject.toml + GitHub Actions(3.7/3.11/3.13 + ruff)。
- 未修：U8 Poller.stop join(5)<wait(30)；U10 仅部分（read_state/write_state、status_pair 仍死代码）。

### 清单外的额外加固（做得好）
- alerts.py: Lock→RLock、deepcopy 返回、_clear_for_key 改列表推导。
- web.py api_flights 修了真实 bug：原「有 poller 就先用内存数据，非今日才拉 API」会在 API 失败时把今日数据当指定日期返回；改为「poller 且 date==today 才用内存」。

### 新发现问题（用户待办）
- **P1-V1 搜索误判**：`_is_stand` 正则 `[A-Z]{1,2}\d{1,2}` 命中所有「2字母+1~2位数字」航班号（CX75/BA15/KA21/HX22/SQ2…），优先级高于航班号匹配且**无兜底回退** → 静默返回 0 条。实测 `query BA15` → 0 结果。建议收紧为 `[WNRSE]\d{1,3}` 或加 0 结果回退。
- **P1-V2**：新增 `TestCursesTUI`（test_hkg_flight.py:798）未加 `skipUnless`，Windows 无 `_curses` → 82 收集 / **2 errors**（本地报红，CI ubuntu 仍绿）。
- P2-V3：`--force` 的 bypass_cache 只作用于 `fetch_airlines`，`fetch_flights` 从不读缓存 → 语义空洞。
- P2-V4：PROJECT-STATUS.md 写「70 个测试」，实际 82 个。

### 评分
安全 B+→A- / 实用 B→B+ / 简洁 C→B+ / 美观 B+→A- / 可维护性 C+→A-。

### 方法论（给自己）
复审增量改动时，先看 `git log <base>..<target>` + `git diff <base>..<target> -- <file>`，再对「新增功能」做**判定矩阵穷举**（把正则/分支的所有输入形态列成表跑一遍）——V1 这种静默误判正是靠穷举矩阵发现的，读代码容易放过。

---

## 复审 v6（基线 03fa20c → 3a110a3，3 个提交）

产出 `CODE-REVIEW-v6.html`。仍为**只读审查**，未改动任何项目文件，全程 bash（未用 PowerShell）。

3 个提交：`b7370c5 fix: distinguish short flight numbers from stands` / `41a6acd chore: address v5 review findings` / `3a110a3 docs: clarify force mode output`。

### 闭环情况：v5 提出的 8 项**全部完成**
- **P1-V1 搜索误判**：`_is_stand` 由 `[A-Z]{1,2}\d{1,2}` 收紧为 `[WNRSEDX]\d{1,3}`。判定矩阵复测：CX75/BA15/KA21/HX22/SQ2 → 航班号 ✅；W63/R13/N30 → 停机位 ✅；G28 → 登机门 ✅；CX → 航司 ✅。
- **P1-V2 curses 测试**：test_hkg_flight.py 加 `importlib.util.find_spec("_curses")` + `@unittest.skipUnless` → 本地 `Ran 88 tests in 1.924s / OK (skipped=2)`。
- 其余 6 项（P2-V3 `--force` 语义、P2-V4 文档测试数、U8 Poller.stop、status_pair 死代码、read_state 部分等）逐项确认。

### 自我更正
v5 中我判 `Poller.stop()` 的 `join(5)`「必然超时」是**言过其实**：`Event.wait()` 一旦 set 立即返回，延迟只取决于是否有在途请求。用户的「加日志 + 返回 False」处理已足够，无需改结构。已在 v6 报告中显式更正。

### 残留（P3，不影响收尾）
- **R1 单字母+数字仍判为停机位**：`D7`（AirAsia X）、`X7`、`E3` → 停机位。正则层面无法彻底区分。建议「**0 结果回退**」：机位/登机门搜索命中 0 条时自动按航班号再搜一次并打印提示（比继续调正则更稳）。
- **R2** `read_state` / `write_state` 仍是零调用死代码（cache.py）。

### 评分（v5 → v6）
安全 A-→A- / 实用 B+→A- / 简洁 B+→A- / 美观 A-→A / 可维护性 A-→A。项目达到**可收尾状态**。
